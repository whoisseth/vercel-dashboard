/** @format */

import React from "react";
import DarkModeBtn from "../DarkModeBtn";

type Props = {};

export default function Footer({}: Props) {
  return (
    <div className="p-8">
      <DarkModeBtn />
    </div>
  );
}
